import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import { CalendarCheck, ChevronRight, Dumbbell, LogOut, ShieldCheck, Sparkles } from "lucide-react";

export default function ClientPortal() {
  const { isAuthenticated, loading, user, logout } = useAuth();
  const workouts = trpc.gym.workouts.useQuery(undefined, { enabled: isAuthenticated });

  if (loading) return <div className="grid min-h-screen place-items-center bg-[#080a0e] text-lime-300"><Dumbbell className="animate-pulse"/></div>;
  if (!isAuthenticated) return <main className="grid min-h-screen place-items-center bg-[#080a0e] p-6 text-white"><div className="max-w-md text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-lime-300 text-black"><Dumbbell/></div><h1 className="mt-7 text-4xl font-semibold tracking-tight">Your training, in one place.</h1><p className="mt-4 text-sm leading-6 text-zinc-500">Sign in to view your membership, attendance history and assigned workout plans.</p><button onClick={()=>startLogin()} className="mt-7 rounded-full bg-lime-300 px-5 py-3 text-sm font-semibold text-black">Sign in to continue <ChevronRight className="ml-1 inline" size={15}/></button></div></main>;

  return <div className="min-h-screen bg-[#080a0e] text-white"><header className="border-b border-white/10"><div className="mx-auto flex max-w-5xl items-center justify-between px-5 py-5"><a href="/" className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-lime-300 text-black"><Dumbbell size={18}/></span><b>GymOS<span className="text-lime-300">.</span></b></a><button onClick={logout} className="flex items-center gap-2 text-sm text-zinc-500 hover:text-white"><LogOut size={15}/> Sign out</button></div></header><main className="mx-auto max-w-5xl px-5 py-10"><p className="text-xs uppercase tracking-[.24em] text-lime-300">Member portal</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Keep your momentum, {user?.name?.split(" ")[0] || "member"}.</h1><p className="mt-3 max-w-xl text-sm leading-6 text-zinc-500">Your progress is built one session at a time. Here is what is waiting for you this week.</p><div className="mt-9 grid gap-4 sm:grid-cols-3"><div className="rounded-3xl border border-white/10 bg-white/[.035] p-5"><CalendarCheck className="text-lime-300" size={20}/><p className="mt-8 text-xs uppercase tracking-[.2em] text-zinc-600">Attendance</p><p className="mt-2 text-2xl font-semibold">—</p><p className="mt-1 text-xs text-zinc-500">Your check-ins will appear here</p></div><div className="rounded-3xl border border-white/10 bg-white/[.035] p-5"><ShieldCheck className="text-sky-300" size={20}/><p className="mt-8 text-xs uppercase tracking-[.2em] text-zinc-600">Membership</p><p className="mt-2 text-2xl font-semibold">Active</p><p className="mt-1 text-xs text-zinc-500">See your gym for plan details</p></div><div className="rounded-3xl border border-white/10 bg-white/[.035] p-5"><Sparkles className="text-amber-300" size={20}/><p className="mt-8 text-xs uppercase tracking-[.2em] text-zinc-600">Consistency</p><p className="mt-2 text-2xl font-semibold">Start</p><p className="mt-1 text-xs text-zinc-500">Your next session is yours</p></div></div><section className="mt-8 rounded-3xl border border-white/10 bg-white/[.035] p-6"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[.2em] text-zinc-600">Training plan</p><h2 className="mt-2 text-xl font-medium">Your assigned workouts</h2></div><Dumbbell className="text-zinc-600" size={20}/></div>{workouts.isLoading?<p className="mt-7 text-sm text-zinc-500">Loading your plan…</p>:workouts.error?<p className="mt-7 text-sm text-red-300">We could not load your plan. Please try again shortly.</p>:workouts.data?.length?<div className="mt-6 grid gap-3 sm:grid-cols-2">{workouts.data.map(w=><div key={w.id} className="rounded-2xl border border-white/10 p-4"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-lime-300/10 text-lime-300"><Dumbbell size={16}/></span><div><p className="font-medium">{w.title}</p><p className="text-xs text-zinc-500">{w.focus || "Strength and conditioning"}</p></div></div></div>)}</div>:<div className="mt-6 rounded-2xl border border-dashed border-white/15 p-7 text-sm text-zinc-500">Your trainer’s plan will appear here once it is assigned.</div>}</section></main></div>;
}
